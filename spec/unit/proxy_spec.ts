import {StabilityProxy} from '../../lib/stabilityproxy'

describe('proxy', () => {
    it('should run a basic test', () => {
        let proxy = new StabilityProxy();
        expect(true);
    });
});
